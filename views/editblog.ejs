<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Website</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white min-h-screen">
    <!-- Header -->
    <header class="bg-gray-800 shadow">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <div class="text-2xl font-bold text-orange-500">BLOG WEBSITE.</div>
            <nav class="space-x-4 flex items-center">
                <a href="#" class="text-orange-500">Home</a>
                <a href="#">About Us</a>
                <a href="#blog-section" class="text-orange-500">Blog</a>
                <a href="#">Authors</a>
                <a href="#">Contact Us</a>
                <a href="/logout" class="bg-orange-500 px-4 py-2 rounded text-white">Logout</a>
            </nav>
        </div>
    </header>

    <!-- Main Section -->
    <div class="container mx-auto px-4 py-8 flex justify-center">
        <!-- Edit Blog Section -->
        <section class="max-w-md w-full">
            <h2 class="text-3xl font-bold mb-6 text-center">Edit Blog</h2>
            <form action="/updateblog" method="POST" class="bg-gray-800 p-6 rounded-lg shadow-md" enctype="multipart/form-data">
                <div class="mb-4">
                    <label for="title" class="block text-gray-300 font-bold mb-2">Blog Title</label>
                    <input type="text" id="title" name="title" value="<%= blogData.title %>" class="w-full px-3 py-2 border rounded-lg bg-gray-700 border-gray-600 text-white" placeholder="Enter blog title" required>
                </div>
                <div class="mb-4">
                    <label for="date" class="block text-gray-300 font-bold mb-2">Date</label>
                    <input type="date" id="date" name="date" value="<%= blogData.date.toISOString().split('T')[0] %>" class="w-full px-3 py-2 border rounded-lg bg-gray-700 border-gray-600 text-white">
                </div>
                <div class="mb-4">
                    <label for="image" class="block text-gray-300 font-bold mb-2">Image</label>
                    <input type="file" id="image" name="image" class="w-full px-3 py-2 border rounded-lg bg-gray-700 border-gray-600 text-white">
                    <img class="w-30 mb-2 rounded" src="<%= blogData.image %>" alt="Current Image">
                </div>
                <div class="mb-4">
                    <label for="content" class="block text-gray-300 font-bold mb-2">Content</label>
                    <textarea id="content" name="description" rows="5" class="w-full px-3 py-2 border rounded-lg bg-gray-700 border-gray-600 text-white" placeholder="Enter blog content" required><%= blogData.description %></textarea>
                </div>
                <div class="flex justify-end">
                    <button type="submit" class="bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600">Update</button>
                </div>
            </form>
        </section>
    </div>
</body>
</html>
